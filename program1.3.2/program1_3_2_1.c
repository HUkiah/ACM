#include<stdio.h>
//#include<stdlib.h>

#define N 1001

int main()
{
	
	int n,TJ[N],KING[N];
	memset(TJ,0,sizeof(TJ)/sizeof(TJ[0]));
	memset(KING,0,sizeof(KING)/sizeof(KING[0]));
	
	while((scanf("%d",&n))!=EOF)
	{
		if(n==0) break;
		


	}
	return 0;
}